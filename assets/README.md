# assets/

- `03_규정문서/` — 규정 PDF 3종(`01_금융소비자보호법`, `03_예금거래기본약관`, `05_상품설명서_정기예금`). 임한빈에게 받아 로컬에 둔다. git에는 넣지 않는다.
- `scenarios/<id>/` — 시연 시나리오. `manifest.json`(pack_version · customer_type · mode) · `audio.wav`(git 제외) · `trace.json`.
