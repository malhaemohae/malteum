# assets/

- `03_규정문서/` — 규정 PDF 7종. 목록·출처·발행 기관은 `03_규정문서/MANIFEST.md`(git 포함). PDF 본체는 임한빈에게 받아 로컬에 둔다. git에는 넣지 않는다. 주담대 시연은 `06_상품설명서_가계대출`, 위험 신호 근거는 `07_제2차_금융분야_보이스피싱_대책` 이 필요하다.
- `scenarios/<id>/` — 시연 시나리오. `manifest.json`(pack_version · customer_type · mode) · `audio.wav`(git 제외) · `trace.json`.
