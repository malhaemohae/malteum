# 시연 시나리오 스크립트 (TTS 제작용)

심사위원은 배포 URL 에 접속해 마이크 없이 서비스를 본다. 그래서 상담 음성을 TTS 로 미리 만들어 두고, 그 음성 파일 하나를 서버로 흘려 STT 부터 판정까지 실제 경로를 태운다. 이 문서는 그 음성의 대본이고, 옆의 `preset-*/script.json` 이 같은 내용의 기계용 사본이다.

팀 합의(2026-09-02) 로 정한 제작·재생 방식은 다음과 같다.

| 단계 | 담당 | 입력 → 출력 |
| --- | --- | --- |
| 대본 | M3 (이 문서) | `script.json` 의 `lines[]`. 줄마다 화자·시각·대사·TTS 발음용 문장·기대 판정 |
| TTS | R5 | 줄마다 클립 하나(`<id>.wav`). 화자별로 다른 음성 |
| 조립 | R5 | 클립을 `start_ms` 타임라인대로 이어 붙여 **단일 파일** `audio.wav` (16kHz · mono · PCM16) |
| 재생 | M1 서버 | `audio.wav` 를 실시간 속도로 STT 에 스트리밍(`replay` 모드) |
| 화자 | M1 서버 | STT 의 화자 분리(diarization: 목소리 차이로 "누가 말했나" 를 구간별로 나누는 기능) 결과를 `teller`·`customer` 로 매핑 |

화자 분리와 매핑은 서버 담당 몫이다. 이 문서는 대본과 조립 규격까지만 정한다.

**대본이 진실 원천이다.** `preset-dep-a/script.json` 에서 계약 fixture `events_scenario_a.json`(trace 재생·리포트·접기 테스트의 입력)을 `back/scripts/gen_scenario_trace.py` 로 만든다. 대사를 바꾸면 `script.json` 을 고치고 → 생성기를 돌려 fixture 를 다시 만들고 → `contracts/validate.py` 와 `pytest` 로 판정 케이스·WS 메시지·엔진 테스트가 같은 대사를 보고 있는지 확인한다. 어느 한쪽만 고치면 시연 화면과 테스트가 서로 다른 상담을 말한다.

| 프리셋 | 팩 | 길이 | 장면 |
| --- | --- | --- | --- |
| `preset-dep-a` | `DEP-2026.08-v4` (ICBC 원화정기예금) | 3분 15초 | 정기예금 중도해지 상담. 기본 시연 경로 |
| `preset-loan-b` | `LOAN-2026.08-v5` (하나은행 가계 신용대출) | 3분 | 신용대출 금리·한도 안내. 심화 경로 |

모든 인물은 가상이다. 화면에 "시연 시나리오" 표시를 상시 띄운다(기획 16장 리스크 4).

## 1. 대본이 화자 분리를 돕기 위해 지킨 것

| 규칙 | 이유 |
| --- | --- |
| 은행원은 남성, 고객은 여성 음성 | 목소리 차이가 클수록 화자 분리가 정확함 `[관례]` |
| 화자가 바뀌는 자리마다 무음 1초 이상(`audio.min_gap_ms`) | 두 화자의 말이 붙으면 STT 가 한 발화로 합쳐 내고, 그러면 문장 단위로 갈라도 화자가 섞임 |
| 같은 화자가 연속으로 말하는 줄은 `speaker` 열이 진실 | 화자 분리 결과와 대조할 때 쓰는 정답. `diarization_hint.turn_order` 참조 |
| 첫 화자는 두 시나리오 모두 고객 | 서버가 "첫 화자 번호 = customer" 같은 단순 규칙을 쓸 수 있게. `diarization_hint.first_speaker` |
| 고객 대사는 되물음 장면 외에 물음표로 끝내지 않음 | 4.2 참조 |

**화자 매핑이 틀리면 무엇이 깨지나.** 위험 신호(`risk`)와 되물음(`comprehension`)은 고객 발화만 보고([tiers/l1/gate.py](../../back/engine/tiers/l1/gate.py)), 필수 고지와 금지 발언은 은행원 발화만 본다. 고객 줄이 은행원으로 붙으면 2:55 의 위험 신호 경보가 사라지고, 반대면 은행원의 고지가 미고지로 남는다.

## 2. 시나리오 A · 정기예금 중도해지 (`preset-dep-a`)

배치 원칙(기획 9장): 전반 정상, 후반으로 갈수록 결함, 첫 결함은 60초 안. 설명의무는 고객의 설명 요청(첫 대사)으로 발동한다(금소법 19조 1항 둘째 분기).

숫자 오류(⑤) 장면은 **세율 15.4%** 로 잡았다. 현행 설명서에서 절대값으로 적힌 유일한 예금 수치이고(p.1), 근거 카드를 열면 그 숫자가 실제로 형광펜에 든다. 중도해지이율은 `약정이율 × 0.5` 같은 비율·산식이라 숫자 대조 대상이 아니다(팀 결정 2026-09-03). 산식을 말했는지는 단어 규칙이 잡고, 내용이 맞는지는 L3(LLM 심판)가 근거 문장을 보고 판단한다.

기대 판정 열의 근거는 4장의 실측이다. `[L3]` 표시는 LLM 심판이 붙어 있을 때만 나오는 결과다.

| 시각 | 화자 | 대사 | 기대 판정 | 장면 |
| --- | --- | --- | --- | --- |
| 0:03 | 고객 | 안녕하세요. 작년에 넣어 둔 정기예금이 있는데, 만기가 아직 안 됐거든요. 지금 해지하면 얼마나 받을 수 있어요? | 없음 | 설명 요청 = 설명의무 발동 |
| 0:12 | 은행원 | 네, 중도해지 기준으로 안내드리겠습니다. 우대이자율은 중도해지하시면 적용이 안 되고, 기본이자율 기준으로 계산됩니다. | `DEP-INT-001` met (L1) | 정상 고지. 첫 항목 전환 |
| 0:30 | 은행원 | 받으시는 이자에는 과세가 되는데요, 세율은 14%입니다. | `DEP-TAX-001` met (L1) + **경보 number_mismatch** (말씀 14% ↔ 설명서 15.4%) | ⑤ 숫자 오류. 첫 결함. 항목은 설명했으니 met, 숫자는 별개 축 |
| 0:42 | 은행원 | 그리고 중도해지하시면 중도해지이율이 적용돼서 이자가 많이 줄어듭니다. | `DEP-INT-002` partial (빠진 요소: 차감률 또는 산출식) + **넛지** "차감률 또는 산출식 안내가 남아 있습니다" `[L3]` | 부분 고지 |
| 0:55 | 고객 | 네에, 중도해지이율이 뭐예요? | `DEP-INT-002` comprehension explained + **재진술 카드** "만기 전에 찾으면 약속한 이자보다 적게 받습니다." | ⑥-B 되물음 감지 |
| 1:05 | 은행원 | 네, 만기 전에 찾으시면 처음 약속한 이자보다 적게 받으신다는 뜻입니다. | 없음 (카드 채택 기록 `[L3]`) | 카드를 따라 말함 |
| 1:15 | 고객 | 아, 중간에 깨면 이자를 훨씬 덜 받는다는 거네요. | `DEP-INT-002` comprehension confirmed `[L3]` | 이해 확인 |
| 1:25 | 은행원 | 만기 지나도 금리가 그대로 유지되니까 그냥 두셔도 돼요. | `DEP-BAN-001` suspected (L2) + **경보 forbidden_phrase** → violated `[L3]` | 금지 발언. 만기후이자율과 모순 |
| 1:37 | 고객 | 그래요. 그럼 그냥 두는 게 낫겠네요. | 없음 | 고객이 틀린 안내를 믿음 |
| 1:45 | 은행원 | 아, 죄송합니다. 정확히 다시 말씀드릴게요. 만기 후에는 약정이율이 아니라 만기후이자율이 적용되고, 경과 기간에 따라 더 낮아집니다. | `DEP-INT-003` met (L1) | 정정 |
| 2:00 | 은행원 | 아까 세금도 정정하겠습니다. 이자에 과세되는 세율은 15.4%입니다. | 없음. 경보 없음 | 숫자 정정. 맞는 숫자엔 경보 0 |
| 2:10 | 은행원 | 그리고 중도해지이율은 약정이율에 예치 기간별 차감률을 곱해서 계산되는데, 석 달 안에 해지하시면 약정이율의 절반 정도만 받으세요. | `DEP-INT-002` met (L1) + 넛지 채택 `[L3]` | 넛지 → 고지 전환. '절반' 이 맞는지는 L3 몫(설명서 1~3개월 약정이율×0.5) |
| 2:25 | 은행원 | 대신 해지하지 않고 예금담보대출을 이용하시면 예금은 그대로 두고 필요한 금액만 빌리실 수 있습니다. | 없음 | 대안 안내(P6 조력) |
| 2:40 | 은행원 | 참고로 이 예금은 예금자보호법에 따라 원금과 이자 포함 1인당 1억 원까지 보호됩니다. | `DEP-PRO-001` partial (빠진 요소: 동일 은행 합산) + 넛지 `[L3]` | 의도된 부분 고지. 끝까지 partial |
| 2:55 | 고객 | 아뇨, 그냥 해지할게요. 해지한 돈은 딸이 알려준 계좌로 바로 보내 주세요. | **경보 risk_signal critical** `DEP-RSK-001` (verdict 없음) | ⑦ 위험 신호 |
| 3:05 | 은행원 | 고객님, 혹시 따님과 직접 통화는 해 보셨어요? 확인 후에 진행하겠습니다. 신분증 먼저 주시겠어요? | 없음 | 마무리 |

**종료 요약(기대)**: 필수 6 중 met 4 (`INT-001`·`TAX-001`·`INT-002`·`INT-003`) · partial 1 (`PRO-001`, 합산 미고지) · unmet 1 (`LIM-001` 일부해지 제한, 끝까지 말하지 않음) · 위반 1 (`BAN-001`, L3 확정) · 경보 3 · 카드 채택 2. 계약 fixture `events_scenario_a.json` 의 종료 요약과 같은 숫자다(같은 대본에서 생성).

심사위원이 직접 눌러 볼 자리(TTS 아님): 0:30 경보 카드의 "근거 원문 보기"(설명서 p.1 의 15.4% 문장에 형광펜) · 종료 후 리포트 PDF 에서 partial 의 빠진 요소 목록 · 위험 신호 경보의 확인(acknowledge) 기록.

## 3. 시나리오 B · 가계 신용대출 금리·한도 안내 (`preset-loan-b`)

기획 9장 B 는 주택담보대출 장면이지만 발행된 대출 팩은 가계 신용대출 하나다. 그래서 대사를 그 팩의 항목에 맞춰 신용대출 상담으로 쓴다. 주담대 고유 항목(LTV·근저당)과 구비서류 안내는 팩에 없어 넣지 않는다(5장 참조).

v5 팩은 항목마다 `l1_patterns` 가 있고 숫자 사실이 셋(연체가산이자율 연 3% · 중도상환해약금 3년 · 청약철회 14일) 있으며, 금리인하요구권(`LOAN-RDR-001`)이 필수 항목으로 들어왔다. 은행원은 이것을 끝까지 말하지 않아 기획 9장 B 표의 '금리인하요구권 누락' 장면이 된다. 아래 기대 판정 열은 **v5 팩** 기준이다.

| 시각 | 화자 | 대사 | 기대 판정 | 장면 |
| --- | --- | --- | --- | --- |
| 0:03 | 고객 | 안녕하세요. 신용대출을 좀 받고 싶은데, 제 조건이면 금리랑 한도가 어느 정도 나올까요? | 없음 | 문의 |
| 0:12 | 은행원 | 네, 먼저 소득과 기존 대출을 확인해서 DSR, 총부채원리금상환비율을 산출하고 심사에 활용합니다. | `LOAN-DSR-001` met (L1) | DSR 고지 |
| 0:25 | 고객 | 네에, DSR이 뭐예요? | `LOAN-DSR-001` comprehension explained + **재진술 카드** "소득에 비해 모든 대출의 원리금을 얼마나 갚아야 하는지 계산해 심사합니다." | ⑥-B 되물음 |
| 0:33 | 은행원 | 소득에 비해 모든 대출의 원리금을 얼마나 갚아야 하는지 계산해서 심사하는 비율입니다. | 없음 | 카드를 따라 말함 |
| 0:45 | 은행원 | 금리 방식은 변동금리라서, 기준금리가 바뀌면 금리가 오를 변동 가능성이 있습니다. | `LOAN-INT-001` met (L1) | 변동금리 위험 고지 |
| 0:57 | 은행원 | 지금 조회한 조건이면 연 4.5% 정도 나오는데, 심사는 무조건 승인됩니다. | `LOAN-BAN-001` suspected (L1) + **경보** → violated `[L3]` | 심사 통과 단정. 첫 결함 |
| 1:10 | 고객 | 아, 그러면 확정인 거네요. | 없음 | |
| 1:16 | 은행원 | 아, 정확히는 내부 결재 전이라 확정은 아니고, 심사 결과에 따라 금리와 한도가 달라질 수 있습니다. | 없음 | 정정 |
| 1:30 | 은행원 | 그리고 상환은 원리금균등으로만 가능합니다. 다른 상환방식은 안 됩니다. | `LOAN-RSK-001` suspected (L1) + **경보** → violated `[L3]` | 상환방식 강요(불공정영업행위) |
| 1:42 | 고객 | 갚는 방식을 제가 고를 수는 없나 보네요. | 없음 | |
| 1:48 | 은행원 | 죄송합니다, 제가 잘못 말씀드렸습니다. 갚는 방법은 고객님이 원하시는 쪽으로 고르실 수 있습니다. | 없음 | 정정. '상환방식' 단어를 피함(예시와 재충돌) |
| 2:00 | 은행원 | 대출을 3년 안에 일찍 갚으시면 중도상환해약금 부과 가능성이 있고, 적용 기간은 대출일부터 3년입니다. | `LOAN-PRE-001` met (L1), 경보 없음(3년 == 설명서) | 중도상환수수료 |
| 2:13 | 은행원 | 연체하시면 대출이자율에 연체가산이자율 연 3%가 더해진 연체이자율이 적용됩니다. | `LOAN-ARR-001` met (L1), 경보 없음(3% == 설명서 p.7) | 연체이자율 산식 + 수치 |
| 2:25 | 은행원 | 대출 실행 후 14일 안에 철회 의사표시를 하시고 대출금 반환을 하시면 청약철회가 됩니다. | `LOAN-WDR-001` met (L1), 경보 없음(14일 == 설명서 p.3) | 청약철회권 |
| 2:40 | 고객 | 신용 조회하면 제 등급이 떨어지는 건 아닌지 걱정되네요. | 없음 | 신용정보 질문 |
| 2:48 | 은행원 | 그 부분은 서류 준비되시면 다시 안내드릴게요. 신분증과 소득 증빙 서류를 준비해 주세요. | 없음 | 마무리. 신용정보·금리인하요구권 미고지 |

**종료 요약(기대)**: 필수 7 중 met 5 · unmet 2 (`LOAN-CIC-001` 신용정보 조회와 동의 · `LOAN-RDR-001` 금리인하요구권) · 위반 2 (`BAN-001`·`RSK-001`, L3 확정) · 경보 2. LLM 이 없으면 두 금지는 suspected 로 남는다. 2:13 의 "연 3%" 를 "연 5%" 로 바꾸면 대출에도 ⑤ 숫자 오류 장면이 생긴다(선택).

## 4. 검증 방법과 실측 기록

### 4.1 엔진 통과 실측 (2026-09-03)

`gen_scenario_trace.py` 가 대본 A 를 서버의 문장 분리(`server/services/stt/assembler.py` 의 `utterances`)를 거쳐 실제 엔진(L0 → L1 → L2, `intfloat/multilingual-e5-small` 임베더, LLM 없음)에 순서대로 넣고, L3 몫은 대본의 `l3` 주석대로 넣어 fixture 48건을 만들었다. 대본 B 는 같은 방식의 세션 스크래치 하네스로 확인했다. STT 를 거치지 않은 정답 전사와 정답 화자 기준이다.

| 확인된 것 | 값 |
| --- | --- |
| A 종료 상태 | met 4 · partial 1 (`PRO-001`, 합산) · unmet 1 (`LIM-001`) · `BAN-001` violated(L3 주석) · 경보 3 · 채택 2. `validate.py` 3층 통과, 엔진 접기·재생 테스트 통과 |
| A 숫자 | "세율은 14%" → `TAX-001` met + 경보(설명서 15.4%). "세율은 15.4%" → 경보 없음 |
| A 금지 발언 L2 예시 유사도 | "만기 지나도 금리가 그대로 유지되니까 그냥 두셔도 돼요" 0.76 (임계 0.5) |
| A 위험 신호 | L1 정규식(`딸이 알려준`) 적중, critical |
| A 산식 | "약정이율에 예치 기간별 차감률을 곱해서 … 절반" → `INT-002` met (L1 `차감률`). 숫자 대조 없음 |
| B 종료 상태 (v5 팩) | met 5 전부 L1 · 0:25 되물음에 `DSR` 재진술 카드 · 3%·3년·14일 에 경보 없음 · `BAN-001`·`RSK-001` suspected(L1) · unmet `CIC`·`RDR` |

### 4.2 대사를 쓸 때 지킨 규칙 (실측에서 나온 것)

| 규칙 | 이유 |
| --- | --- |
| 고객 대사는 되물음 장면 외에 **물음표로 끝내지 않는다** | 엔진 기본 되물음 패턴에 `\?\s*$` 가 있어, 물음표로 끝나는 고객 발화는 전부 되물음으로 잡히고 이미 설명된 항목 중 하나에 재진술 카드가 붙는다 |
| "네?" 를 **독립 문장으로 두지 않는다** ("네에, …" 로 이어 쓴다) | 서버가 `?` 뒤에서 문장을 나누므로 "네?" 만 따로 판정된다. 유사도가 전부 0 인데도 되물음 대상이 첫 항목으로 잡혀 엉뚱한 재진술 카드가 떴다(실측) |
| 은행원 대사 한 문장에 **한 항목만** 담는다 | 서버가 마침표 단위로 나눠 판정한다 |
| 요소가 하나뿐인 키워드 언급은 판정되지 않는다 | `matcher.required_verdict` 가 요소 1개짜리 일반 키워드 히트를 "주제 언급" 으로 보고 넘긴다. 단 그 항목의 숫자가 같은 문장에 있으면 판정한다(0:30 의 세율이 그 경우) |
| partial 을 남기려면 요소를 **정확히 하나** 빼고 말한다 | `PRO-001` 은 '합산'·'합쳐'·'합해' 중 하나가 들어가면 met 이 된다. 그래서 "원금과 이자 포함" 으로 썼다 |
| 정정 문장에서 금지 예시의 단어를 다시 쓰지 않는다 | "상환방식은 고객님이 선택하실 수 있습니다" 는 금지 예시와 0.66 겹쳐 재경보 위험. "갚는 방법" 으로 바꿨다 |
| 정정할 때 다른 숫자를 함께 말하지 않는다 | 2:00 에 "15.4%" 만 말한다. "14% 에 지방소득세 1.4%" 를 덧붙이면 그 둘도 세율 사실과 대조돼 경보가 뜬다 |

### 4.3 TTS 제작과 조립 지침

| 항목 | 내용 |
| --- | --- |
| 클립 | 대사 한 줄에 파일 하나(`<id>.wav`). `tts_text` 가 있으면 그것을 TTS 에 넣고, 없으면 `text` 그대로 |
| 음성 | `speakers` 의 설명대로 은행원 남성·고객 여성. 두 시나리오의 은행원은 같은 음성 |
| 숫자 발음 | "십사 퍼센트"·"십오 점 사 퍼센트"·"일억 원"·"디에스알"·"십사 일" 처럼 STT 가 숫자·용어로 되돌리기 쉬운 발음으로 `tts_text` 에 적었다 |
| 조립 | 클립을 `start_ms` 에 놓아 하나의 `audio.wav` 로 만든다. 앞 클립이 아직 안 끝났으면 앞 클립 끝 + `min_gap_ms`(1초) 에 놓고 이후 줄을 같은 만큼 민다 |
| 출력 규격 | 16kHz · mono · PCM16 WAV. 계약 `audioFrame` 규격이고 서버가 다른 규격을 거절한다(`stt/audio.py`) |
| 위치 | `assets/scenarios/<preset_id>/audio.wav`. git 에는 넣지 않는다(`assets/README.md`) |
| 여백 | 경보 카드 뒤(A 0:30, 1:25 / B 0:57, 1:30) 는 심사위원이 읽을 시간으로 10초 이상 비워 두었다 |
| 공급자 | 미결(기획 19장). 생성 음성의 상업적 사용 약관을 확인한다 |

### 4.4 STT 사전 점검 (조립된 음원이 나오면 반드시)

`back/scripts/stt_check.py` 가 재는 방식 그대로, 조립된 `audio.wav` 를 Deepgram 에 흘려 아래가 전사에 그대로 나오는지, 그리고 줄마다 화자가 `script.json` 의 `speaker` 와 같은지 본다. 하나라도 어긋나면 그 줄의 판정이 통째로 빠진다.

| 줄 | 전사에 있어야 하는 것 | 없으면 |
| --- | --- | --- |
| 전체 | 줄마다 화자 번호가 `speaker` 열과 일관되게 대응 | 고객 줄이 은행원으로 붙으면 위험 신호·되물음이 사라짐 |
| A 0:12 | `우대이자율` `기본이자율` (붙여서) | `INT-001` 미고지 |
| A 0:30 | `과세` `14%` (또는 `14 퍼센트`) | 숫자 경보 없음, `TAX-001` 미고지 |
| A 0:42 | `중도해지` `중도해지이율` | `INT-002` partial 안 뜸 |
| A 0:55 | `중도해지이율이 뭐예요` | 되물음 미검출 → 재진술 카드 없음 |
| A 2:00 | `과세` `15.4%` | 정정이 기록되지 않음(경보는 어차피 없음) |
| A 2:10 | `중도해지이율` `차감률` | `INT-002` 가 partial 로 남음 |
| A 2:40 | `예금자보호` `1억` (숫자 1) | "일억" 으로 오면 한도 요소가 빠져 partial 도 안 되고 L3 몫이 됨 |
| A 2:55 | `딸이 알려준 계좌` + 화자 customer | 위험 신호 미검출 |
| B 0:12 | `DSR` 또는 `총부채원리금상환비율` | `DSR-001` 미고지 |
| B 0:57 | `무조건 승인됩니다` | 금지 발언 미검출 |
| B 1:30 | `다른 상환방식은 안` | 금지 발언 미검출 |
| B 2:13 | `연체가산이자율` `3%` | 숫자가 다르게 오면 오경보 |

keyterm 은 팩의 `jargon_terms` 를 서버가 자동으로 넣는다(`ws/endpoint.py` 의 `_start_stt`). `차감률`·`중도해지이율` 이 keyterm 에 있어 붙여 쓴 전사가 나올 확률이 높지만, 실측 전까지는 가정이다.

## 5. 알려진 제약

| 제약 | 내용 | 영향 |
| --- | --- | --- |
| 화자 분리 | 서버가 STT 화자 분리 옵션을 켜고 번호를 `teller`·`customer` 로 매핑해야 한다. 현재 서버는 오디오 경로의 화자를 전부 `teller` 로 고정(`stt/session.py`) | 서버 작업 전에는 고객 줄의 위험 신호·되물음이 뜨지 않음 |
| L3 의존 | 넛지, violated 확정, comprehension confirmed, 카드 채택 기록은 LLM 심판이 붙어야 나온다 | `APP_LLM_MODEL` 이 비면 그 장면이 빠진다. 배포 전 확인 |
| 산식 판단 | 중도해지이율·만기후이자율의 비율(`약정이율×0.5`)은 숫자 대조가 아니라 L3 몫 | 은행원이 "절반" 을 "70%" 로 잘못 말해도 L1·L2 는 경보를 내지 않음. L3 가 근거 문장과 대조해야 함 |
| 용어 밀도(⑧) | 최근 8발화 창 안 은행원 발화의 팩 `jargon_terms` 출현 수로 잼(`engine/state/term_density.py`, 2026-09-03 dev 병합). 3개 이상이면 high, 은행원 발화 3턴 동안 0개면 low | 대본 A 는 0:42 부터 high 로 올라가 1:45 근처에서 low·normal 을 스치고 2:10 이후 다시 high. 경보가 아니라 `progress.term_density` 상태 표시 |
| 대출 팩 | 주담대가 아니라 신용대출 팩. 서류 항목(`LOAN-DOC-001`)은 설명서에 구비서류 목록이 없어 검토 대기 | B 는 기획 9장 B 표의 서류 안내 장면을 담지 못함. LTV·근저당은 주담대 팩 몫 |
| 서버 프리셋 | `GET /presets` 가 `assets/scenarios/*/script.json` 을 읽어 대본 하나를 프리셋 하나로 냄(`server/services/presets.py`, 2026-09-03 dev 병합). `preset_id`·`title`·`mode`·`product_code`·`pack_version`·`customer_profile`·`duration_ms` 를 그대로 쓰고, `lines[].expect` 의 둘째 토큰(`number_mismatch`·`rephrase` 등)을 세어 `expected_highlights` 를 만듦 | **`expect` 문자열의 앞 두 토큰(`alert <type>` / `assist <type>`)은 서버가 읽는 형식이 됨.** 바꾸면 심사위원 화면의 '볼 것' 안내가 바뀐다. `audio.wav` 가 없으면 `audio_ref` 를 내지 않아 재생 버튼이 켜지지 않음 |

## 6. DB 적재 (첫 배포와 팩 재발행 뒤)

2026-09-03 기준 배포된 서버·DB 는 아직 없다. 처음 띄울 때 `make up` 다음에 `make seed` 를 한 번 돌린다. 저장소의 팩 파일 전부(`contracts/fixtures/rulepack_*.json`)와 시연 A 이벤트 48건을 DB 에 넣는다.

- 팩 판정 자체는 DB 가 비어도 돈다. 서버가 파일로 내려가고(`DbThenFilePackSource`) L2 임베딩은 메모리에서 즉석 계산한다. 그러나 `GET /packs`·`GET /packs/{version}` 은 DB 만 읽어 빈 목록·404 가 되고, trace 재생(기획 11.4)은 저장된 이벤트가 없으면 재생할 것이 없다. `make seed` 는 이 둘을 채운다
- 팩이 DB 에 들어간 뒤로는 그 버전은 DB 가 파일을 이긴다. 파일만 고치면 화면은 옛 값을 보여주므로, 팩을 재발행하면 `make seed` 를 다시 돈다(`--replace` 라 같은 버전을 덮어쓴다)
- `--unsigned` 는 fixtures 파일이 서명 없는 발행물이라 필요하다. 서명 적재(`compile` envelope + `RULEPACK_APPROVAL_HMAC_KEY`)는 `rulepack/docs/PIPELINE.md`
- 프리셋 목록은 `script.json` 의 `pack_version` 을 그대로 내므로 따로 넣거나 넘길 것은 없다
