import data from './landing-preview-data.json';

// Derived from contract fixtures, checked by scripts/landing-qa.cjs.
// Keep the deployable front/ context self-contained. Never inject into a live session.
export const landingPreview = data;
