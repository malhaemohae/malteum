# AGENTS.md — 저장소 전체 규칙

이 문서는 사람과 코딩 에이전트가 이 저장소에서 지켜야 할 원칙이다. 모듈별 규칙은 각 폴더의 `AGENTS.md`에 있고, 폴더 규칙이 이 문서보다 우선한다.

## 원칙

1. **파일 경계 = 사람 경계.** `back/server`(M1 노순혁) · `back/engine`(M2 허현준) · `back/rulepack`(M3 임한빈) · `front`(M4 서재오). 자기 폴더 밖의 코드는 고치지 않고, 필요하면 담당자에게 요청한다. 경계는 import-linter가 기계적으로 검사한다.
2. **접점은 `back/contracts/` 하나다.** 스키마·fixtures·`engine_contract.py`만 지키면 서로의 코드를 볼 일이 없다. 계약은 2026-08-28 동결이며 그 이후 변경은 전원 합의. 계약을 바꾸려면 fixtures를 함께 고치고 `validate.py`를 통과시킨다.
3. **정본은 하나다.** 런타임 모델은 스키마에서 생성한다(`back/server/generated/`, 수동 편집 금지). 상태는 이벤트를 접은 파생물이고 이벤트가 원본이다. 이름·주소·설정은 한 곳(`settings`)에만 둔다.
4. **근거 없는 문장을 만들지 않는다(P4).** 화면에 나가는 인용·답변은 팩의 `evidence`에서 온다. 근거를 못 찾으면 `None`을 돌려주고 침묵한다. 숫자는 어느 단계에서도 모델이 고치지 않고 경보로 넘긴다.

## 작업 방식

- 빈 파일·빈 폴더를 미리 만들지 않는다. 첫 파일이 생길 때 폴더를 만든다.
- 커밋 전에 `make test`(ruff · lint-imports · pytest)가 통과해야 한다.
- 브랜치: `main`(배포) · `dev`(통합) · `feat/<module>-<topic>`. PR은 `dev`로.
- 서비스명은 코드·패키지명에 쓰지 않는다. 제품 표시 이름은 `settings.APP_DISPLAY_NAME` 한 곳.
