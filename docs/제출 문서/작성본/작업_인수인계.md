# 제출 문서 작업 인수인계

2026-09-06 사용자 요청으로 중단했음. 아래 상태부터 이어서 진행할 것.

## 최우선 지시

- 기획서와 기능명세서는 상위 폴더의 양식 PDF·HWPX를 기준으로 작성
- 팀명 말해모해. 임한빈(팀장), 노순혁, 서재오, 허현준 순서
- 개조식·음슴체. 문서에서 아직 구현되지 않은 기능을 현재 기능으로 표시하지 않음
- diagram-design은 https://github.com/cathrynlavery/diagram-design 의 기본 밝은 스타일만 사용
- 프로젝트 색상·Pretendard를 적용한 기존 도식은 사용자가 거절했음. 색상만 바꾸는 수준으로 끝내지 말고 기본 템플릿의 서체·여백·가느다란 선·정보 밀도로 다시 구성
- 화면 캡처는 최종 지시로 생략. 촬영할 화면·강조 영역·조건 설명만 유지
- 새 규정 문서 업로드 → 자동 룰팩 갱신은 MVP 제외
- 실제 홈서버는 아직 미배포. URL·모델 연결·실제 실행 성공을 지어내지 않음
- 사용자는 토큰 부족으로 지금 중단을 요청했음. 자동으로 재개하거나 추가 에이전트를 실행하지 않음

## 완료한 것

- 양식의 파란 가이드와 필수 항목 확인. 기획서 1~7번, 기능명세서 1~5번 유지
- 엔진 담당자의 두 아키텍처 문서와 Mermaid 구조 확인
- `build_documents.py`: 기획서 6쪽·기능명세서 13쪽의 본문과 편집 HTML 생성기 작성
- `render_documents.cjs`: Chrome으로 PDF 출력·페이지별 QA 이미지·넘침 검사 구현
- 첫 출력 PDF 2종·HTML 2종 존재. 첫 레이아웃 검사에서 넘침 0건
- `내일_교체_체크리스트.md`: 화면별 촬영 절차, 빨간 박스·확대 지침, 배포 후 채울 항목 작성
- 독립 심사 관점 검토 1차 수행 후 본문 일부 수정

## 2026-09-06 재개 후 상태 (Claude)

- 도식 5종을 diagram-design 기본 스킨으로 전면 재작성함. 생성기는 `assets/diagrams/build_diagrams.py`(옛 `build-diagrams.cjs` 삭제), 렌더는 `render-diagrams.cjs`. 5종 모두 글자 넘침·겹침 0건 [실측 render-check.json]
- 도식 배치: 05 사용 흐름 → 기능명세서 3번, 01 전체 구성 · 02 엔진 판정 · 03 전사 교정 · 04 룰팩 파이프라인 → 4번
- `build_documents.py` → `render_documents.cjs` 재실행. 기획서 6쪽 · 기능명세서 13쪽, 레이아웃 넘침 0건. 두 PDF 가 현재 본문 기준 최신
- 화면 캡처는 사용자 지시로 생략. 촬영 안내 상자 유지(기능명세서 2·3·5번)
- 서브에이전트 심사 반복(10점 목표)은 토큰 부족으로 수행하지 않음. 1차 독립 평가 반영분까지만 반영된 상태
- 커밋하지 않음. 현재 브랜치가 `feature/remove-click-outline` 이라 문서 커밋을 섞지 않았음. 문서용 브랜치를 따로 만들어 `docs/제출 문서/작성본` 과 `docs/엔진 아키텍처` 를 올릴지 사용자가 결정

## 2026-09-06 심사 반영 2회전 (Claude)

독립 심사 평가와 구현 대조를 각각 받아 2회전 반영함. 1차 5.5 → 2차 6.5.
2차 평가는 배포 URL·화면 5장이 채워지는 것을 전제로 한 점수이고, 지금 PDF
그대로 내면 4.5 라고 했음. 남은 감점의 대부분이 미배포와 빈칸임.

### 반영이 끝난 것

- **안 도는 시험 발화 3건 교체.** 엔진에 직접 넣어 확인함. 옛 문장은 판정·경보
  가 하나도 안 나서 심사위원 화면이 비었음
- 구현 상태 범례를 계약 테스트 기준으로. 관련 화면 이름 8개를 실제 라벨로
- 기능명세서 5번에 계약 테스트 17건 요약표·대체 확인 경로·A7 신설
- 기획서 5번에 모델명(OpenAI Realtime · Qwen3-8B · Sortformer)과 LLM 두 용처
- 기획서 2번 요약에 검증 현황 한 줄, 7번 자유 항목을 실측 표로
- 백엔드 테스트 326 → 512(실측), 오탐 회피 5 → 8건
- 사용 흐름 도식을 기획서 4번으로, 도식 1 번호 회복

### 남은 것

| 항목 | 왜 안 했나 |
|---|---|
| 배포 URL·계정·녹화 링크·저장소 주소 | 배포 후 기입. 문서에 [입력 필요] 로 표시 |
| 화면 캡처 5장 | 사용자 지시로 생략. 촬영 안내만 유지 |
| 반쪽 빈 페이지 7곳 | 두 쪽 채움 합이 100%를 넘어 병합 불가. 캡처가 들어가면 자연히 채워짐 |
| 전사 교정 쪽 삭제 | 심사위원은 권고했으나 엔진 아키텍처 2종을 각각 그리라는 원 지시가 우선 |
| 시장 규모 수치 | 검증된 공개 통계를 못 찾음. 지어내지 않음 |

### 제출 전 반드시

- 팩을 다시 발행하지 말 것. 시연 음원 목록이 최신 팩에 묶여 있어 예금 v7 을
  발행하면 예시 음원 시험이 통째로 막힘
- 문서의 517·17·8 은 `cd back && pytest -q` 와 `judge_cases.json` 로 검산됨.
  이 중 517 은 시험을 하나 더 넣을 때마다 바뀌는 값임. 제출 직전에 다시 재고,
  문서 3곳(기획서 2번 요약 · 기능명세서 1번 · 5번)을 함께 고칠 것

## 현재 산출물 주의
- `assets/screens/`는 이전 지시하에 촬영된 로컬 자료이나 최신 지시로 문서에 사용하지 않음. figure 함수는 화면 자리에 촬영 안내를 출력하도록 수정했음
- QA 이미지·캡처는 작성본 `.gitignore`에서 제외. 제출물로 넘길 필요 없음

## 다음 실행 순서

1. 도식 5종 기본 스타일 재작성: 전체 구성, 엔진 판정, 전사 교정, 룰팩 구축, 사용자 흐름
2. 기본 스킬 `SKILL.md`와 `assets/template.html`, `example-architecture.html`, 해당 유형 reference를 재사용. HTML+SVG+PNG 보관
3. 도식의 글자·선·교차·상태 의미를 확인하고 self_check 실행. 모든 도식을 실제로 열어 시각 확인
4. 본문 수정분과 사실 관계 재검토. 특히 기본 서버에서 문서 본문 전체 검색 미연결, LLM 보조 교정 기본 비활성 유지
5. `build_documents.py` → `render_documents.cjs` 실행. 두 PDF 전체 페이지를 시각 검토하고 실제 페이지 수·잘림·폰트 확인
6. 독립 검토 피드백 재평가. 배포·실측 부재를 숨겨 10점을 만들지 말 것
7. 최종 결과 링크와 남은 배포 항목을 간단히 전달

## 검토 결과와 반영

1차 독립 평가 6.5/10은 심사자의 추정 점수이며 실제 심사 결과 아님. 화면 캡처 생략은 승인된 범위라 감점하지 않았고 도식 시각 평가는 제외했음.

| 지적 | 현재 조치 |
|---|---|
| URL·접속 조건 부재 | 미배포 표기와 체크리스트 유지. 배포 후 입력 필요 |
| 코드 존재와 실제 작동을 혼동 | 구현 상태에 검증 범위 명시. 추가로 확인 환경·실제 결과 구분 보강 가능 |
| 시험 합격 기준 모호 | DEP-v6·직원·14% 대 15.4%·DEP-TAX-001, LOAN-v7·LOAN-BAN-001 명시 |
| 실제 모델 구성 불명확 | Qwen3-8B·e5-small·Sortformer를 기존 실험 구성으로만 추가. 배포 모델은 미확인 |
| 문서 전체 검색 과장 가능 | 팩 항목·연결 근거 범위로 정정. 기본 서버 chunks 미연결 명시 |
| 평가 계획 통과선 없음 | 예금·대출 각 20건, 2인 정답 검토, 초기 목표를 미측정 계획으로 추가 |
| 경쟁 서비스 비교 근거 약함 | 접근 방식 비교라는 한계 명시. 특정 경쟁사의 기능 부재를 주장하지 않음 |

## 확인한 구현 근거

- `back/server/bootstrap/startup.py`의 `_adapters`: chunks를 주입하지 않음
- `back/engine/build.py`: chunks 기본값 None
- `back/server/bootstrap/settings.py`: llm_corrector=False, llm_generator=False. 모델·임베딩은 설정 의존
- `back/engine/tiers/l1/numeric.py`: 아라비아 숫자와 단위를 정규식으로 대조. 한글 수사·분리 발화 보완 PR 전체 적용을 주장하지 않음
- 최신 계약 fixture: DEP-2026.08-v6 12항목, LOAN-2026.08-v7 15항목 직접 집계
- 사용자 원문 엔진 자료는 설계 자료임. 모든 내용이 현재 기본 서버 연결을 보장하지 않으므로 코드와 대조 필요

## 최신 실행 검증

- ruff check 통과, ruff format 222파일 통과, import-linter 5계약 유지
- pytest: 517 passed, 4 skipped, 62 warnings, 41.35초 (2026-09-06 재측정)
- Git Bash `scripts/gen_models.sh --check`: generated/ 최신, 종료코드 0
- 위 결과는 백엔드 회귀 검사이며 실서버 STT·LLM 성공이나 모델 정확도를 증명하지 않음

## 환경과 Git

- 현재 권한 unrestricted. 명령에 sandbox_permissions 넣지 말 것
- Python: `C:/Users/hanbin/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`
- Node: `C:/Users/hanbin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`
- Playwright: 같은 runtime의 `dependencies/node/node_modules/playwright`, Chrome channel 사용
- Word·LibreOffice 변환기 없음. 편집 원본은 자체 편집·그림 교체·저장 기능을 가진 HTML로 결정했음
- PDF artifact 시작 마커는 성공적으로 실행했음. 중복 실행 필요 없음
- 작업 시작 시 feature/submission-documents를 만들었으나 다른 작업으로 현재 feature/remove-click-outline로 변경돼 있음. 현재 브랜치를 임의로 전환하거나 다른 변경을 되돌리지 말 것
- 이번 문서 변경은 커밋하지 않았음. docs/엔진 아키텍처·제출 양식은 사용자가 넣은 미추적 원본이므로 소유권 구분
- 중단 때문에 기능 단위가 닫히지 않은 WIP임. 재개 후 검증을 마친 뒤 별도 문서 브랜치 또는 격리 작업에서 커밋 정리

## 에이전트

- Dalton 01a07309-670f-73e3-bd68-5e1c8eba340d: 도식 초안·리디자인. 중단 요청 보냈음
- McClintock 01a07315-7796-7941-80eb-c8e5499a2f54: 독립 심사 검토. 2차 검토 도중 중단 요청 보냈음
- Feynman 01a07305-a361-7303-a493-6e7da163099c: 구현 대조. 마지막 초안 읽기는 권한 오류로 못 했으므로 전체검증 완료로 취급 금지
- Arendt 01a07306-c457-79d1-9721-5408e932b966: 이전 로컬 캡처. 추가 촬영 중단 요청 완료

## 2026-09-06 엔진 담당자 자료 반영 (Claude)

허현준이 보낸 세 문서(`docs/말틈 엔진 전체 구성과 에이전트 역할.md`,
`docs/말틈 에이전트별 담당 역할과 동작 과정.md`, `docs/말틈 엔진 실험 결과.md`)를
읽고, 원본 실험 기록 `origin/feat/engine-numeric-context` 의
`back/engine/experiments/numeric_context/RESULT.md` 로 수치를 직접 검산한 뒤 반영함.

### 고친 사실 오류

| 자리 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 기획서 5번 음성 인식 | OpenAI Realtime 으로 변환 | 두 갈래를 밝힘. 품질 비교는 상용, 내부망 확인 실행은 로컬 Qwen3-ASR 1.7B |
| 기획서 7번 재판정 지연 | 3초 제한 초과 0건 | 음원 재생에서는 0건. 제한을 푼 반복 실험에서는 최대 3.94초 |
| 기능명세서 4번 실행 구성 | 모델 나열만 | 어디서 돌렸는지 구분. 로컬 3종 + 외부 판정 1종 |

두 번째 줄이 특히 중요함. 엔진 담당자 자료가 `12/12 일치를 모든 요청이 3초 안에
성공했다는 의미로 사용하지 않는다` 고 못박아 두었는데, 우리 문서만 단정하고 있었음.
심사위원이 두 자료를 나란히 보면 과장으로 읽혔을 자리임.

### 새로 넣은 것

- 기획서 7번을 두 쪽으로 나눔. 음성 처리 위치를 정한 실측 / 판정 모델과 숫자 처리
- **내부망 이전의 실물 근거**: 그래픽카드 한 장짜리 PC 에서 음성 인식·화자 분리·검색을
  돌려 예금 147초·대출 128초를 끝까지 처리했고 항목별 결과가 대본 기대값과 일치.
  기존 문서는 `화자 구간으로 끊는 것을 전제 조건으로 확정` 까지만 적고 그 전제가
  통했다는 결과가 빠져 있었음
- 큰 모델을 안 쓰는 이유: 320억 개짜리는 재생 중 시간 초과 9회로 위반 확정을 놓쳤고
  반복 정답 9/12. 80억 개짜리는 12/12
- 규칙 판정 속도 실측(느린 쪽 5% 포함 0.3ms 미만)을 5ms 상한 옆에 붙임
- 숫자 처리 개선: 값·단위 추출 4/36 → 36/36, 잘못된 세율 경보 3/12 → 12/12
- 기능명세서 4번에 규정 질문 답변 절 신설. 주요 기능 목록에는 있는데 처리 방식
  설명이 없던 자리임. 근거 밖 수치가 섞인 답변을 버린다는 것이 핵심
- 받아쓴 문장 교정에 실제 사례(`차감눌` → `차감률`) 추가

### 반영하지 않은 것

| 항목 | 왜 |
|---|---|
| 엔진 브랜치의 회귀 검사 560건 | 아직 병합 전. 우리 브랜치 기준은 517 이며 병합되면 이 숫자도 같이 고쳐야 함 |
| 답변용 320억 개 모델 검토 | 연결과 성능 검증이 끝나지 않은 계획이라 문서에 넣지 않음 |
| 세 에이전트로 5번 전면 재구성 | 원 구조를 유지하고 `지금은 꺼 둔 두 자리` 한 줄로 정리. 전면 재구성은 지시받지 않았음 |

### 쪽수

기획서 9쪽 → 10쪽, 기능명세서 15쪽 → 16쪽. 레이아웃 넘침 0건.

## 2026-09-06 시연 음원 재제작(Typecast) 영향

시연 음원을 ElevenLabs 에서 Typecast(`ssfm-v30`)로 다시 뽑았음.
`back/scripts/make_scenario_audio.py` 가 이미 Typecast 로 만들도록 쓰여 있고,
`--write-start` 가 `start_ms`·`duration_ms` 를 되쓴 상태임.

### 길이가 크게 달라졌음

| 시나리오 | 옛 음원 | 새 음원 |
|---|---:|---:|
| preset-dep-a (예금 중도해지) | 147.43초 | 113.18초 |
| preset-loan-b (신용대출) | 127.80초 | 105.21초 |
| preset-dep-d (예금 신규) | 없음 | 123.53초 |
| preset-loan-c (대출 대환) | 없음 | 121.88초 |

시연 음원이 2편에서 4편으로 늘었음. 기능명세서 5번 B 표의 예시 음원 항목에
네 편을 안내할지는 음원이 커밋된 뒤 결정할 것.

### 문서에서 이미 뺀 것

기획서 7번의 `147초·128초` 표기 두 곳을 지웠음. 그 숫자는 옛 파일에만 맞는
값이고, 심사위원이 지금 시연을 돌리면 다른 길이를 봄.

### 새 음원으로 다시 재야 하는 값

아래는 전부 옛 ElevenLabs 음원으로 잰 값임. 목소리가 바뀌면 음성 인식 결과가
달라지므로 그대로 두면 안 됨. 재실행 뒤 이 표를 채워 문서에 반영할 것.

| 문서 자리 | 지금 값 | 재실행 필요 |
|---|---|---|
| 기획서 7번 음성 인식 비교 | 오류율 3.6% · 0.4%, 용어 15/15 | 예 |
| 기획서 7번 화자 구분 | 평균 0.61초 · 최대 1.09초, 32줄 정확 | 예 |
| 기획서 7번 내부망 완주 | 대본 기대 요약과 일치 | 예 |
| 기획서 7번 재판정 지연 | 최소 1.24 · 평균 1.64 · 최대 2.06초 | 예 |
| 기획서 7번 숫자 처리 | 4/36 → 36/36 등 | 아니오. 저장된 전사문 12벌 비교라 음원과 무관 |

### 함께 손봐야 하는 것

- `script.json` 의 `audio` 블록에 `tts_model` 이 `ssfm-v30` 으로 갱신됐는지,
  `gap_ms`·`alert_gap_ms` 가 새 조립 규칙과 맞는지 확인. 스크립트 기본값은
  `gap_ms` 1300 · `alert_gap_ms` 2200 인데 옛 `script.json` 은 1200 · 4000 이었음
- `--write-start` 로 `start_ms` 를 되쓴 뒤에는 `gen_scenario_trace.py` 로
  계약 fixture 이벤트를 다시 만들어야 함. 스크립트 주석에 그 순서가 있음
- 화자가 바뀌는 자리의 무음이 `min_gap_ms`(1초) 아래로 내려가면 두 사람의 말이
  한 발화로 붙음. `speaker.py` 의 `NEAREST_GAP_MS` 가 1초라서 그럼

## 커밋 이력 주의 (2026-09-06)

- `9fd152d` 는 메시지가 용어 복원만 말하지만 기획서 7번의 대본 기대 요약 수치
  정정도 함께 담고 있음. 공유 브랜치라 rebase 로 메시지를 고치지 않았음
- 이 브랜치는 여러 세션이 동시에 커밋함. `git commit --amend` 는 공유 인덱스를
  삼켜 남의 커밋을 덮어쓸 수 있으므로 쓰지 말 것. 실제로 한 번 덮어써서
  같은 내용·메시지로 복구했음(`91a8bb8` → `2599573`)

## 2026-09-06 PR #45 병합 이후 갱신 (Claude)

`feat/scenario-tts-typecast` 가 병합되고 프론트 개선이 여러 건 들어온 뒤 문서와
코드가 어긋난 자리를 훑어 반영함.

### 고친 사실

| 자리 | 옛 값 | 새 값 | 근거 |
|---|---|---|---|
| 서버 코드 시험 3곳 | 517건 | 603건 | `pytest -q` 재실행 |
| 계약 시험 L3 상한 | 1500ms | 3000ms | `judge_cases.json` 의 `expect.max_ms` |
| 시연 상담 | 음원 2편 | 네 편 | `assets/scenarios/` 에 네 편 모두 커밋됨 |

603 이 된 이유는 시험이 늘어서가 아니라 **막혀 있던 38건이 살아나서**임.
`Path.read_text()` 를 인코딩 없이 부르던 6곳 때문에 한글 Windows(cp949)에서
`UnicodeDecodeError` 가 났음. 리눅스 CI 는 UTF-8 이라 통과해 아무도 못 봤음.
`fix(test): 한글 Windows 에서 시험 38건이 깨지던 인코딩 문제` 로 함께 고침.

### 새로 넣은 기능 설명

**전문용어 밀도**가 기능 목록에 없었음. 규정팩의 `jargon_terms` 목록으로 은행원
최근 발화의 용어 수를 세어 낮음·보통·높음으로 표시하는 게이지임. 경보가 아니라
상태 표시이고, 규칙으로만 세므로 모델 없이 동작함. 값이 높아지면 쉬운 말 안내가
뜨는 계기(⑥-B 트리거) 중 하나임. 상담 화면 위 도구 줄의 칩으로 나감.

### 시연 음원 새 길이 (Typecast `ssfm-v30`)

| 시나리오 | 길이 |
|---|---:|
| preset-dep-a 예금 중도해지 | 114.60초 |
| preset-loan-b 신용대출 | 105.66초 |
| preset-dep-d 예금 신규 | 126.37초 |
| preset-loan-c 대출 대환 | 123.30초 |

앞선 회차에서 잰 값(113.18 · 105.21 · 123.53 · 121.88)과 다름. 그 사이 한 번 더
뽑았다는 뜻이므로, 문서에 초 단위를 다시 넣지 말 것.

### 도식

5종을 1000x352~480 에서 **720x720 정사각형**으로 재배치함. 세로 연결선 라벨을
선 위에 얹던 자리를 옆으로 8px 띄웠음(diagram-design §6 규칙 2). 범례는 좁아진
폭에서 두 줄로 접힘. 넘침 0 · 겹침 0 · 스킬 `self_check.py` 전건 OK.

### 아직 안 본 것

프론트에 리포트 개선이 여럿 들어왔음(판정 상태를 색과 기호로 · 요약과 출처를
문서 카드로 · 타임라인 스크롤 · 자동 쉬운 말을 발화 블록 안에). 기능명세서의
기능 목록 수준에서는 이미 덮이는 내용이라 따로 넣지 않았음. 화면 캡처를 새로
찍을 때 이 변화가 반영된 화면인지 확인할 것.
